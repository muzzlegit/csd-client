import styled from '@emotion/styled';

export const SearchContainer = styled.div(
  {
    margin: '0',
    padding: '8px',
    width: '50%',
    alignItems: 'center',
    outline: '1px dashed tomato'
  }
);
