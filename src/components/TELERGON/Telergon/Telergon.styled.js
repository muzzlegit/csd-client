import styled from '@emotion/styled';

export const Container = styled.div(
  {
    margin: '20px 0',
    padding: '0 8px',
    width: 'auto',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'start',
    gap: '12px'
  }
);
